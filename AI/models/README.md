# Models Directory

This directory stores trained ML model artifacts for the PS-9 AI Service.

## Contents

- `duplicate_classifier.joblib` — Trained logistic regression for duplicate/related/separate classification
- `duplicate_classifier_eval.json` — Evaluation metrics from training

Models are generated by running `python -m scripts.train_duplicate_model` from the AI root.
